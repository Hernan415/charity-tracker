<!-- views/donations-show.handlebars -->

<div class="row mt-4">
  <div class="col-lg-6">
    <img class="img-fluid w-100 rounded mb-3" src="{{donation.imgUrl}}" alt="Card image cap">
    <small class="text-muted">Created on: {{donation.createdAtFormatted}}</small>
    <h3>{{donation.title}}</h3>
    <div class="lead">{{donation.desc}}</div>
    <div class="text-right">
      <a href="/donations/{{donation.id}}/edit">Edit</a>
    </div>
  </div>
  <div class="col-lg-4 offset-lg-1">
    <div class="d-flex justify-content-between">
        <b>RSVPs ({{donation.Rsvps.length}})</b>
        <a href="/donations/{{donation.id}}/rsvps/new" class="btn btn-primary btn-sm">RSVP</a>
    </div>
    <div class="list-group mt-4">
      {{#each donation.Rsvps}}
        <div class="list-group-item clearfix">
          <div class="float-left">
            {{this.name}}
          </div>
          <div class="float-right">
            <form class="form-inline" action="/donations/{{../donation.id}}/rsvps/{{this.id}}?_method=DELETE" method="post">
              <button type="submit" class="btn btn-link btn-sm">Remove</button>
            </form>
          </div>
        </div>
      {{/each}}
    </div>
</div>
</div>
