/*!
 * depd
 * Copyright(c) 2015 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module exports.
 * @public
 */

module.exports = donationListenerCount

/**
 * Get the count of listeners on an donation emitter of a specific type.
 */

function donationListenerCount (emitter, type) {
  return emitter.listeners(type).length
}
